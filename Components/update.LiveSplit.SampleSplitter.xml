<updates>

  <!-- Applied only when `version` is greater than `SampleSplitterFactory.Version`. -->
  <update version="0.0.0">
    <files>
      <!-- Path to the updated file relative to `SampleSplitterFactory.UpdateURL`. -->
      <!-- Possible values for `status` are: `added`, `changed`, `removed`. -->
      <file path="Components/LiveSplit.SampleSplitter.dll" status="changed"/>
    </files>

    <!-- Bullet point list of changes in this update. -->
    <changelog>
      <change>Added a sample feature</change>
    </changelog>
  </update>

</updates>
